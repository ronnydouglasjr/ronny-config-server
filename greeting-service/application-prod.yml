spring:
  cloud:
    config:
      enabled: false
  application:
    name: greeting-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

greeting-service:
  greeting: "Ciao"
  default-value: "Mondo"
